# Issue #1

```bash
/app # python3 generator.py /shared/resources/m2m-cms-swagger.json ra --output-dir=/shared/generated/demo/react-admin --module-name="A Pet Admin" --rest-server-url=localhost:3000/api/v1
Loading specification file...
Using spec format 'json'
Generating main JS component file...
Generating ReactAdmin.js file...
ReactAdmin.js

        If you get schema validation errors from a yaml Swagger spec that passes validation on other
        validators, it may be because of single apostrophe's (') used in some descriptions. The
        parser used does not like it at all.
```